diff --git a/src/App.tsx b/src/App.tsx
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -6,6 +6,7 @@
 import EvidenceScreen from './screens/EvidenceScreen';
 import ConversationFlowScreen from './screens/ConversationFlowScreen';
 import CommitmentBuilderScreen from './screens/CommitmentBuilderScreen';
+import ClosingScreen from './screens/ClosingScreen';
 import ScoringScreen from './screens/ScoringScreen';
 import FeedbackScreen from './screens/FeedbackScreen';
 
@@ -26,6 +27,9 @@
       case 'commitment_builder':
         return <CommitmentBuilderScreen />;
 
+      case 'closing':
+        return <ClosingScreen />;
+
       case 'scoring':
         return <ScoringScreen />;
 
diff --git a/src/engine/branchingEngine.ts b/src/engine/branchingEngine.ts
--- a/src/engine/branchingEngine.ts
+++ b/src/engine/branchingEngine.ts
@@ -22,9 +22,9 @@
     'short answers',
     'high formality',
     'minimal context',
-    'it’s fine',
+    "it's fine",
     'okay',
-    'i don’t know',
+    "i don't know",
     'brief reply',
   ],
 
@@ -32,10 +32,10 @@
     'precise dates',
     'workload references',
     'evidence-based counterpoints',
-    'that’s not correct',
+    "that's not correct",
     'actually',
-    'the report wasn’t late',
-    'the segmentation error wasn’t mine',
+    "the report wasn't late",
+    "the segmentation error wasn't mine",
   ],
 
   partial_openness: [
@@ -44,7 +44,7 @@
     'i felt',
     'i was disappointed',
     'the promotion',
-    'i don’t feel valued',
+    "i don't feel valued",
   ],
 
   polite_disengagement: [
diff --git a/src/engine/closingEngine.ts b/src/engine/closingEngine.ts
--- a/src/engine/closingEngine.ts
+++ b/src/engine/closingEngine.ts
@@ -20,11 +20,11 @@
 
 function detectFalseReassurance(msg: string): boolean {
   const reassuranceSignals = [
-    'you’ll be fine',
+    "you'll be fine",
     'everything is okay',
-    'don’t worry',
+    "don't worry",
     'it will all work out',
-    'you’re doing great',
+    "you're doing great",
   ];
   const lower = msg.toLowerCase();
   return reassuranceSignals.some((s) => lower.includes(s));
@@ -35,7 +35,7 @@
     'next promotion',
     'you will be promoted',
     'i will get you promoted',
-    'i promise you’ll move up',
+    "i promise you'll move up",
     'your promotion is coming',
   ];
   const lower = msg.toLowerCase();
@@ -45,6 +45,7 @@
 function detectFollowUp(msg: string): boolean {
   const followUpSignals = [
     'follow-up',
+    'follow up',
     'check-in',
     'next meeting',
     'review together',
diff --git a/src/engine/commitmentEngine.ts b/src/engine/commitmentEngine.ts
--- a/src/engine/commitmentEngine.ts
+++ b/src/engine/commitmentEngine.ts
@@ -17,6 +17,8 @@
   manager: CommitmentValidation;
   employee: CommitmentValidation;
   allComplete: boolean;
+  valid: boolean;
+  error: string | null;
 }
 
 // -----------------------------
@@ -72,11 +74,22 @@
 ): DualCommitmentValidation {
   const manager = validateSingleCommitment(managerCommitment);
   const employee = validateSingleCommitment(employeeCommitment);
+  const allComplete = manager.isComplete && employee.isComplete;
+
+  const problems: string[] = [];
+  if (!manager.isComplete) {
+    problems.push('Manager commitment missing: ' + manager.missingFields.join(', '));
+  }
+  if (!employee.isComplete) {
+    problems.push('Employee commitment missing: ' + employee.missingFields.join(', '));
+  }
 
   return {
     manager,
     employee,
-    allComplete: manager.isComplete && employee.isComplete,
+    allComplete,
+    valid: allComplete,
+    error: problems.length > 0 ? problems.join(' | ') : null,
   };
 }
 
diff --git a/src/engine/scoringEngine.ts b/src/engine/scoringEngine.ts
--- a/src/engine/scoringEngine.ts
+++ b/src/engine/scoringEngine.ts
@@ -1,4 +1,4 @@
-import { ConversationStage, ConversationBranch } from '../store/conversationState';
+import { ConversationStage, ConversationBranch, ScoreResult } from '../store/conversationState';
 
 export interface ScoringInput {
   clarity: number;
@@ -16,7 +16,7 @@
   directness: number;
 }
 
-export function scoreMessage(input: ScoringInput): ScoringResult {
+export function scoreMessage(input: ScoringInput): ScoreResult {
   const { clarity, empathy, directness } = input;
 
   const avg = (clarity + empathy + directness) / 3;
diff --git a/src/screens/ConversationFlowScreen.tsx b/src/screens/ConversationFlowScreen.tsx
--- a/src/screens/ConversationFlowScreen.tsx
+++ b/src/screens/ConversationFlowScreen.tsx
@@ -53,9 +53,9 @@
       return;
     }
 
-    // If we reached closing → go to ScoringScreen
+    // If we reached closing → go to ClosingScreen
     if (result.nextStage === 'closing') {
-      setCurrentScreen('scoring');
+      setCurrentScreen('closing');
       return;
     }
 
diff --git a/src/screens/ScenarioOverviewScreen.tsx b/src/screens/ScenarioOverviewScreen.tsx
--- a/src/screens/ScenarioOverviewScreen.tsx
+++ b/src/screens/ScenarioOverviewScreen.tsx
@@ -7,10 +7,11 @@
   const { setCurrentScreen } = useNavigationState();
 
   const metadata = {
-    scenario_id: scenarioData.scenaria_id,
+    scenario_id: scenarioData.scenario_id,
     title: scenarioData.title,
     difficulty: scenarioData.difficulty,
-    primary_competency: scenarioData.primary_competency
+    competency: scenarioData.primary_competency,
+    stakes: scenarioData.stakes
   };
 
   const managerProfile = scenarioData.manager_profile;
@@ -25,17 +26,6 @@
       </p>
       <p style={styles.competency}>Competency: {metadata.competency}</p>
 
-      {/* Emotional Tone */}
-      <div style={styles.section}>
-        <h2 style={styles.sectionTitle}>Emotional Tone</h2>
-        <ul>
-          <li><strong>Start:</strong> {metadata.emotional_tone.start}</li>
-          <li><strong>Mid:</strong> {metadata.emotional_tone.mid}</li>
-          <li><strong>Constructive End:</strong> {metadata.emotional_tone.end_constructive}</li>
-          <li><strong>Deteriorated End:</strong> {metadata.emotional_tone.end_deteriorated}</li>
-        </ul>
-      </div>
-
       {/* Profiles */}
       <div style={styles.row}>
         <ProfileCard title="Manager" profile={managerProfile} />