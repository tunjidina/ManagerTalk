<?xml version="1.0" encoding="utf-8"?>
<!--
    Adaptive-icon background layer.

    A shape drawable rather than a flat colour so the launcher icon matches
    the 512x512 store icon, which carries the same top-to-bottom lift. The
    foreground PNGs are transparent apart from the bubbles, so this is what
    fills the whole 108dp canvas and survives whatever mask the launcher
    applies.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="#085593"
        android:endColor="#0B477A" />
</shape>
